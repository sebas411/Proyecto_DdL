(* string WHITESPACE = "ws";
   string IF = "if";
   string ELSE = "else";
   vector<string> tokens; *)


let delim = [' ''\t''\n']
let ws = delim+

rule tokens = 
    ws        { tokens.push_back(WHITESPACE); }
    | "if"    { tokens.push_back(IF); }
    | "else"  { tokens.push_back(ELSE); }
    
(*
for (string token: tokens) {
  cout << "El token es: " << token << endl;
}
*)
