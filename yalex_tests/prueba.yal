(* hola como estas este es un comentario *)

(* aqui tenemos otro comentario *)

let hola = "Como estas"

let quionia = "Jerry"

rule tokens = 
    ws
  | id        { return ID }               (* Cambie por una accion valida, que devuelva el token *)
  | '+'       { return PLUS }
  | '*'       { return TIMES }
  | '('       { return LPAREN }
  | ')'       { return RPAREN }
